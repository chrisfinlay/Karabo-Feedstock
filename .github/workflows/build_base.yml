name: Build Base

env:
  # Native Global Libs
  FFTW3_VERSION: 3.3.10
  FFTW3F_VERSION: 3.3.10

  # Rascil Line
  PYBDSF_VERSION: 1.14.1
  RASCIL_VERSION: 2.1.0 #1.0.0
  SKA_SDP_DTMDL_VERSION: 1.4.1 #1.0.0
  SKA_TELMODEL_VERSION: 1.33.0
  SKA_SDP_FUNC_PY_VERSION: 1.0.0  #0.1.4
  SKA_SDP_FUNC_VERSION: 1.0.0 #0.0.6

  # Oskar Line
  OSKAR_VERSION: 2.12.2
  OSKARPY_VERSION: 2.12.2  # oskarpy should have same version as `OSKAR_VERSION`

  # casacore possible combinations from https://anaconda.org/conda-forge/casacore/files
  # | casacore | build     | boost   | hdf5    | numpy    | cfitsio |
  # |    3.7.1 |           |   =1.86 | =1.14.6 |   >=1.19 |  =4.6.2 |
  # |    3.7.0 | _0        |   =1.86 | =1.14.3 |   >=1.19 |  =4.5.0 |
  # |    3.6.1 | _4        |   =1.86 | =1.14.4 |   >=1.19 |  =4.5.0 |
  # |    3.6.1 | _0        |   =1.86 | =1.14.3 |   >=1.19 |  =4.4.1 |
  # |    3.5.0 | _0        | =1.74.1 | =1.12.1 | >=1.21.6 |  =4.0.0 |
  # |    3.5.0 | nompi_117 |   =1.84 | =1.14.3 | >=1.22.4 |  =4.4.0 |
  # |    3.5.0 | nompi_115 |   =1.82 | =1.14.3 | >=1.22.4 |  =4.4.0 |
  # |    3.5.0 | nompi_114 |   =1.82 | =1.14.2 | >=1.22.4 |  =4.3.1 | *this one!
  # |    3.5.0 | nompi_110 |   =1.78 | =1.14.1 | >=1.21.6 |  =4.3.0 |
  CASACORE_VERSION: 3.7.1
  BOOST_VERSION:    1.88.0
  HDF5_VERSION:     1.14.6
  CFITSIO_VERSION:  4.6.2

  # hvox Line -> don't add dev-label through ci-options because the setup is not ready for that.
  HVOX_VERSION: 0.0.1.dev48  # `pbr` version because they don't have an official release tag yet
  PYCSOU_VERSION: 1.0.7.dev1679  # `pbr` version because they just refer to `v2-dev` branch which has this version

  # WSClean
  IDG_VERSION: 1.2.0
  EVERYBEAM_VERSION: 0.8.0
  WSCLEAN_VERSION: 3.6.0


  # additional karabo dependencies
  AOTOOLS_VERSION: 1.0.7
  SEQFILE_VERSION: 0.2.0
  EIDOS_VERSION: 1.1.0
  SKA_GRDR_NFTY_CU_VERSION: 0.3.0
  KATBEAM_VERSION: 0.1.0 # acutal version 0.1 and has therefore no patch, but is needed for conda-build
  TOOLS_21CM_VERSION: 2.4.3 # actual version 2.0, patch needed for correct scipy version
  ARATMOSPY_VERSION: 1.0.0
  PYUVDATA_VERSION: 2.4.5  # Karabo pins >=2.4,<3; conda-forge has no 2.x for linux-aarch64
  MONTAGEPY_VERSION: 6.0.0 # version is pinned through python-wheel in conda_build_config.yaml

  # SKACH contributions
  FINUFFT_VERSION: 2.2.0
  BLUEBILD_VERSION: 1.0.0

  # Other
  PACKAGE_NAME: ${{ inputs.packageName }}
  CHANNEL_LABEL: ${{ inputs.label }}
  # anaconda.org channel to solve against and (with ANACONDA_SECRET) upload to.
  # Forks set the repository variable ANACONDA_CHANNEL to their own channel.
  CHANNEL: ${{ vars.ANACONDA_CHANNEL || 'i4ds' }}

on:
  workflow_call:
    inputs:

      packageName:
        required: true
        type: string

      packageVersion:
        required: true
        type: string

      packageVersionName:  # env.VAR_NAME of the corresponding build
        required: true
        type: string

      buildNumber:
        required: true
        type: string

      customPath:
        required: false
        type: string

      pythonCall:
        required: false
        default: ""
        type: string

      label:
        required: false
        default: "main"
        type: string

      dev:
        required: false
        default: false
        type: boolean

      devDeps:  # string of dev-deps with ", " as delimiter
        required: false
        default: ""  # set matching packages-names for dev to use
        type: string

      useCuda:
        required: false
        default: false
        type: boolean

      isNative:
        required: false
        default: false
        type: boolean

      skipTest:
        required: false
        default: false
        type: boolean

jobs:
  build_base:
    # Native (compiled) recipes are built on both x86-64 and ARM64 runners; noarch recipes
    # only once, so the same file is not uploaded twice.
    strategy:
      fail-fast: false
      matrix:
        os: ${{ fromJSON(inputs.isNative && '["ubuntu-latest", "ubuntu-24.04-arm"]' || '["ubuntu-latest"]') }}
    runs-on: ${{ matrix.os }}
    outputs:
      pkgv: ${{ steps.pkgv.outputs.version }}
      uploaded: ${{ steps.token.outputs.present }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: Install Conda
        uses: conda-incubator/setup-miniconda@v3
        with:
          auto-update-conda: true
      # The CUDA recipes take nvcc and the CUDA libraries from conda-forge (cuda-nvcc,
      # cuda-cudart-dev, ...), so no toolkit or compiler is installed on the runner.
      - name: Free disk space on runner
        if: ${{ inputs.useCuda }}
        run: |
          sudo rm -rf /usr/local/lib/android /opt/ghc /usr/share/dotnet || true
          docker system prune -af || true
          df -h
      - name: Set custom path to recipe
        if: inputs.customPath != ''
        run: |
          echo "recipePath=${{ inputs.customPath }}" >> "$GITHUB_ENV"
      - name: Set default path to recipe
        if: inputs.customPath == ''
        run: |
          echo "recipePath=${{ inputs.packageName }}/." >> "$GITHUB_ENV"
      - name: Prepare Setup
        # In case ${{ inputs.dev }}="true", ${{ inputs.packageVersion }} of ${{ inputs.packageVersion }}
        # gets a PEP440 dev-version added at position "patch".
        # In case ${{ inputs.dev }}="true" & ${{ inputs.devDeps }} is not empty, the in `env` declared variables
        # included in `devDeps` gets a PEP440 dev-version added and exported for the underlying conda-build process.
        id: pkgv
        run: |
          BUILD_NR=${{ inputs.buildNumber }}
          if [[ ${{ inputs.dev }} == "true" ]]
          then
            BUILD_NR="$(($BUILD_NR + 1000))"
            PKG_VERSION=$(echo "${{ inputs.packageVersion }}" | sed 's/\([0-9]*\.[0-9]*\.\)\([0-9]*\)/\1dev\2/g')
            DEV_DEPS_LIST=$(echo "${{ inputs.packageVersionName }}, ${{ inputs.devDeps }}")
          else
            PKG_VERSION=${{ inputs.packageVersion }}
            DEV_DEPS_LIST=""
          fi
          echo "build=$BUILD_NR" >> "$GITHUB_ENV"
          echo "version=$PKG_VERSION" >> $GITHUB_OUTPUT


          PKG_KEYS=$(echo '${{ toJSON(env) }}' | jq -r 'keys | .[]')
          for PKG_KEY in $PKG_KEYS; do
            if [[ $DEV_DEPS_LIST == *"$PKG_KEY"* ]]
            then
              VERSION_ALT=$(echo "${!PKG_KEY}" | sed 's/\([0-9]*\.[0-9]*\.\)\([0-9]*\)/\1dev\2/g')
            else
              VERSION_ALT=${!PKG_KEY}
            fi
            NAME_ALT=${PKG_KEY}_ALT
            echo "$NAME_ALT=$VERSION_ALT" >> "$GITHUB_ENV"
          done
        # install conda-build is changed with version 25. the target is swtiched to .conda  instead of .tar.bz2
        # We explicitly add conda-forge and i4ds to the global conda config.
        # Reason: Since conda-build 25, the solver no longer automatically inherits
        # channels from the CLI `conda build -c ...` call when resolving *pinned*
        # dependencies inside the build environment.
        # Conda-build 25 no longer inherits CLI channels → we now configure channels explicitly
      - name: Install Conda-Build and create target
        run: |
          conda install -y -n base conda-libmamba-solver
          conda config --set solver libmamba
          conda config --remove-key channels || true # remove all existing channels

          conda config --add channels conda-forge
          conda config --add channels ${{ env.CHANNEL }}/label/${{ inputs.label }}
          conda config --add channels ${{ env.CHANNEL }}
          conda config --add channels defaults
          conda config --set channel_priority flexible
          conda install "conda-build>=25"

          conda build -c ${{ env.CHANNEL }}/label/${{ inputs.label }} -c ${{ env.CHANNEL }} -c conda-forge ${{ env.recipePath }}

      - name: Set native platform as architecture
        if: ${{ inputs.isNative }}
        shell: bash -l {0}
        run: |
          echo "arch=$(conda info --json | jq -r .platform)" >> $GITHUB_ENV
      - name: Set python as architecture
        if: ${{ !inputs.isNative }}
        run: |
          echo "arch=noarch" >> $GITHUB_ENV
      - name: Check for an anaconda.org token
        # Forks without ANACONDA_SECRET can still build a package that has no feedstock
        # dependencies (and see the build fail or pass); the upload and the install-from-channel
        # test are skipped. Packages that depend on other feedstock packages (wsclean, oskarpy,
        # ska-sdp-datamodels, ska-sdp-func-python) need those uploaded first, i.e. a token.
        id: token
        env:
          TOKEN: ${{ secrets.ANACONDA_SECRET }}
        run: |
          if [[ -n "$TOKEN" ]]; then echo "present=true" >> "$GITHUB_OUTPUT"; else echo "present=false" >> "$GITHUB_OUTPUT"; echo "::notice::ANACONDA_SECRET not set - build only, no upload, no install test; dependent packages need uploads"; fi
      - name: Install and publish to Conda
        if: steps.token.outputs.present == 'true'
        shell: bash -l {0}
        run: |
          conda activate base
          conda install anaconda-client
          anaconda -t ${{ secrets.ANACONDA_SECRET }} upload "$(conda info --base)/conda-bld/${{ env.arch }}/${{ inputs.packageName }}"*.conda --label ${{ inputs.label }}


  test_base:
    needs: build_base
    # The test installs the package from the channel, so it needs the upload to have happened.
    if: ${{ !inputs.skipTest && needs.build_base.outputs.uploaded == 'true' }}
    strategy:
      fail-fast: false
      matrix:
        # noarch packages are built once but must install on every platform, so always test both.
        os: ["ubuntu-latest", "ubuntu-24.04-arm"]
    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Conda
        uses: conda-incubator/setup-miniconda@v3
        with:
          auto-update-conda: true

      - name: Install Mamba Solver
        run: |
          conda install -y -n base conda-libmamba-solver
          conda config --set solver libmamba

      - name: Free disk space on runner (test)
        if: ${{ inputs.useCuda && !inputs.skipTest }}
        run: |
          sudo rm -rf /usr/local/lib/android /opt/ghc /usr/share/dotnet || true
          docker system prune -af || true
          df -h

      # Karabo is Python 3.12 only (Karabo-Pipeline #700); several recipes build for 3.12 only.
      - name: Install Package and test with Python 3.12
        shell: bash -l {0}
        run: |
          conda create -y -n testenv12 python=3.12
          conda activate testenv12
          conda install -y -c ${{ env.CHANNEL }}/label/${{ inputs.label }} -c ${{ env.CHANNEL }} -c conda-forge ${{ inputs.packageName }}="${{ needs.build_base.outputs.pkgv }}"
          python -c "${{ inputs.pythonCall }}"

